# 前端接口文档

> 本文档由分析代码路由和控制层自动生成，包含了当前所有的 API 接口、请求参数与响应。

## 模块: `chat`

### POST `/api/chat`
- **处理函数**: `Chat`
- **所在文件**: `api/ai_api/chat.go`
- **请求参数**:
  - 绑定结构体: `UserAIRequest`
  ```go
  Messages  []openai.ChatCompletionMessage `json:"messages"`   // 对话历史消息
  UserInput string                         `json:"user_input"` // 用户输入
  ImageID   uint                           `json:"image_ID"`   // 图片ID（可选）//TODO:加入图片的支持功能
  Model     string                         `json:"model"`      // 模型名称（可选，默认使用全局配置）
  ```
- **成功响应**:
  - `OkWithData(result)`

---
### POST `/api/chat/send`
- **处理函数**: `ChatSendView`
- **所在文件**: `api/chat_api/chat.go`
- **请求参数**:
  - 绑定结构体: `ChatSendRequest`
  ```go
  Msg       models.ChatMsg `json:"msg"`
  RevUserID uint           `json:"revUserID"` // 接收用户ID
  ```
- **成功响应**:
  - *返回默认成功或未检测到标准 response 响应*

---
### GET `/api/chat/get`
- **处理函数**: `ChatListView`
- **所在文件**: `api/chat_api/chat.go`
- **请求参数**:
  - 绑定结构体: `ChatListRequest`
  ```go
  common.PageInfo
  UserID uint `form:"userID" binding:"required"` // 查我和他的聊天记录
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### GET `/api/chat/session`
- **处理函数**: `SessionListView`
- **所在文件**: `api/chat_api/chat.go`
- **请求参数**:
  - 绑定结构体: `SessionListRequest`
  ```go
  common.PageInfo
  ```
- **成功响应**:
  - `OkWithList(list)`

---
## 模块: `article`

### POST `/api/article`
- **处理函数**: `ArticleCreateView`
- **所在文件**: `api/article_api/article_create.go`
- **请求参数**:
  - 绑定结构体: `ArticleCreateRequest`
  ```go
  Title       string        `json:"title" binding:"required"`   // 文章标题，最大32字符
  Abstract    string        `json:"abstract"`                   // 文章摘要，最大256字符
  Content     string        `json:"content" binding:"required"` // 文章内容
  CategoryID  *uint         `json:"categoryID"`                 // 文章分类ID，关联分类表
  TagList     []string      `json:"tagList"`                    // 标签列表，JSON序列化存储 //serializer:json要删掉?似乎要换成自己定义的taglist数据类型
  Cover       string        `json:"cover"`                      // 文章封面图片URL
  OpenComment bool          `json:"openComment"`                // 是否开启评论：true-开启 false-关闭
  Stats       models.Status `json:"status"`                     //状态,普通用户只能设置为草稿或者审核中,管理员可设置为任意值
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("文章创建成功)`

---
### PUT `/api/article`
- **处理函数**: `ArticleUpdateView`
- **所在文件**: `api/article_api/article_update.go`
- **请求参数**:
  - 绑定结构体: `ArticleUpdateRequest`
  ```go
  ID          uint     `json:"id" binding:"required"`
  Title       string   `json:"title" binding:"required"`
  Abstract    string   `json:"abstract"` //要考虑一个问题,如果用户想设置为空简介,那么就设为"该文章未设置简介"
  Content     string   `json:"content" binding:"required"`
  CategoryID  *uint    `json:"categoryID"`
  TagList     []string `json:"tagList"`
  Cover       string   `json:"cover"`
  OpenComment bool     `json:"openComment"`
  ```
- **成功响应**:
  - `OkWithMsg("文章更新成功)`

---
### PUT `/api/article/inc`
- **处理函数**: `ArticleUpdateView2`
- **所在文件**: `api/article_api/article_update.go`
- **请求参数**:
  - 绑定结构体: `ArticleUpdateRequest2`
  ```go
  ID          uint      `json:"id" binding:"required"`
  Title       *string   `json:"title"`
  Abstract    *string   `json:"abstract"` //要考虑一个问题,如果用户想设置为空简介,那么就设为"该文章未设置简介"
  Content     *string   `json:"content"`
  CategoryID  *uint     `json:"categoryID"`
  TagList     *[]string `json:"tagList"`
  Cover       *string   `json:"cover"`
  OpenComment *bool     `json:"openComment"`
  ```
- **成功响应**:
  - `OkWithMsg("未做任何修改")`

---
### GET `/api/article`
- **处理函数**: `ArticleListView`
- **所在文件**: `api/article_api/article_list.go`
- **请求参数**:
  - 绑定结构体: `ArticleListRequest`
  ```go
  common.PageInfo
  Type       string        `form:"type" binding:"required"`
  UserID     uint          `form:"userID"`
  CategoryID *uint         `form:"categoryID"`
  Status     models.Status `form:"status"`
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### GET `/api/article/search`
- **处理函数**: `ArticleSearchView`
- **所在文件**: `api/article_api/article_search.go`
- **请求参数**:
  - 绑定结构体: `ArticleSearchRequest`
  ```go
  common.PageInfo
  Tag  string `form:"tag"`  // 按标签筛选
  Type int8   `form:"type"` // 排序类型: 0 最新发布 1 猜你喜欢    2最多回复 3最多点赞 4最多收藏
  ```
- **成功响应**:
  - `OkWithList([]ArticleSearchListResponse{})`

---
### GET `/api/article/:id`
- **处理函数**: `ArticleDetailView`
- **所在文件**: `api/article_api/article_detail.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithData(result)`

---
### POST `/api/article/top/:id`
- **处理函数**: `ArticleTopView`
- **所在文件**: `api/article_api/article_top.go`
- **请求参数**:
  - 绑定结构体: `ArticleTopRequest`
  ```go
  TopType   string `json:"topType"`                      //,置顶类型，用户置顶还是管理员置顶,不填写时默认为用户置顶
  ArticleID uint   `json:"articleID" binding:"required"` //文章ID
  ```
- **成功响应**:
  - `OkWithMsg("置顶成功")`

---
### DELETE `/api/article/top`
- **处理函数**: `ArticleCancleTopView`
- **所在文件**: `api/article_api/article_top.go`
- **请求参数**:
  - 绑定结构体: `ArticleTopRequest`
  ```go
  TopType   string `json:"topType"`                      //,置顶类型，用户置顶还是管理员置顶,不填写时默认为用户置顶
  ArticleID uint   `json:"articleID" binding:"required"` //文章ID
  ```
- **成功响应**:
  - `OkWithMsg("取消置顶成功")`

---
### DELETE `/api/article/admingTop`
- **处理函数**: `AdminArticleDeleteView`
- **所在文件**: `api/article_api/article_top.go`
- **请求参数**:
  - 绑定结构体: `AdminArticleTopRequest`
  ```go
  UserID    uint `json:"userID"`
  ArticleID uint `json:"articleID"`
  ```
- **成功响应**:
  - `OkWithMsg("取消置顶成功")`

---
### GET `/api/article/review`
- **处理函数**: `ArticleReviewListView`
- **所在文件**: `api/article_api/article_review.go`
- **请求参数**:
  - 绑定结构体: `ArticleReviewListViewRequest`
  ```go
  common.PageInfo
  UserID uint `form:"userID"` //可以指定选择谁的文章
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### POST `/api/article/review/:id`
- **处理函数**: `ArticleReviewView`
- **所在文件**: `api/article_api/article_review.go`
- **请求参数**:
  - 绑定结构体: `ArticleReviewRequest`
  ```go
  ArticleID uint          `json:"articleID" binding:"required"`
  Status    models.Status `json:"status" binding:"required"` //审核状态,2为通过,1,3为不通过
  Msg       string        `json:"msg"`                       // 为4的时候，传递进来
  ```
- **成功响应**:
  - `OkWithMsg("审核成功")`

---
### POST `/api/article/look`
- **处理函数**: `ArticleLookView`
- **所在文件**: `api/article_api/article_history.go`
- **请求参数**:
  - 绑定结构体: `ArticleLookRequest`
  ```go
  ArticleID  uint `json:"articleID" binding:"required"`
  TimeSecond int  `json:"timeSecond"` // 读文章一共用了多久,预备字段,也许以后可以用于做喜好分析
  ```
- **成功响应**:
  - `OkWithMsg("未登录")`

---
### POST `/api/article/digg/:id`
- **处理函数**: `ArticleDiggView`
- **所在文件**: `api/article_api/article_digg.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("点赞成功")`

---
### DELETE `/api/article`
- **处理函数**: `ArticleRemoveUserView`
- **所在文件**: `api/article_api/article_remove.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("成功删除 %d 篇文章")`

---
### DELETE `/api/article/admin`
- **处理函数**: `ArticleRemoveView`
- **所在文件**: `api/article_api/article_remove.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("删除成功 成功删除%d条")`

---
### GET `/api/article/history`
- **处理函数**: `ArticleLookListView`
- **所在文件**: `api/article_api/article_history.go`
- **请求参数**:
  - 绑定结构体: `ArticleLookListRequest`
  ```go
  common.PageInfo
  UserID uint `form:"userID"` //为0时是查询自己的浏览记录,否则是指定用户的浏览记录
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### DELETE `/api/article/history`
- **处理函数**: `ArticleLookRemoveView`
- **所在文件**: `api/article_api/article_history.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("删除历史记录成功 共删除%d条")`

---
### POST `/api/article/category`
- **处理函数**: `CategoryCreateView`
- **所在文件**: `api/article_api/article_category.go`
- **请求参数**:
  - 绑定结构体: `CategoryCreateRequest`
  ```go
  ID    uint   `json:"id"` //id为0时为创建，否则为修改
  Title string `json:"title" binding:"required,max=32"`
  ```
- **成功响应**:
  - `OkWithMsg("创建分类成功")`

---
### GET `/api/article/category`
- **处理函数**: `CategoryListView`
- **所在文件**: `api/article_api/article_category.go`
- **请求参数**:
  - 绑定结构体: `CategoryListRequest`
  ```go
  common.PageInfo
  UserID uint   `form:"userID"`
  Type   string `form:"type" binding:"required"` // self 查自己 other 查别人 admin 后台
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### DELETE `/api/article/category`
- **处理函数**: `CategoryRemoveView`
- **所在文件**: `api/article_api/article_category.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithMsg(msg)`

---
### POST `/api/article/collect`
- **处理函数**: `ArticleCollectView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `ArticleCollectRequest`
  ```go
  ArticleID uint `json:"articleID" binding:"required"`
  CollectID uint `json:"collectID"`
  ```
- **成功响应**:
  - `OkWithMsg("收藏成功")`

---
### GET `/api/article/collect/folder`
- **处理函数**: `CollectListView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `CollectListViewRequest`
  ```go
  common.PageInfo
  ID uint `form:"id"`
  ```
- **成功响应**:
  - `OkWithList(data)`

---
### GET `/api/article/collect/list`
- **处理函数**: `CollectArticleListView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `CollectArticleListViewRequest`
  ```go
  common.PageInfo
  Likes []string `form:"likes"`
  ID    uint     `form:"id"`
  ```
- **成功响应**:
  - `OkWithList(data)`

---
### POST `/api/article/collect/folder`
- **处理函数**: `CollectCreateView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `CollectCreateRequest`
  ```go
  Title    string `json:"title" binding:"required,max=32" s:"title"`
  Abstract string `json:"abstract" s:"abstract"`
  Cover    string `json:"cover" s:"cover"`
  ```
- **成功响应**:
  - `OkWithMsg("创建收藏夹成功")`

---
### PUT `/api/article/collect/folder`
- **处理函数**: `CollectUpdateView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `CollectUpdateRequest`
  ```go
  ID       uint    `json:"id" binding:"required"` //更新时需要传id参数
  Title    *string `json:"title" `
  Abstract *string `json:"abstract"`
  Cover    *string `json:"cover"`
  ```
- **成功响应**:
  - `OkWithMsg("更新收藏夹成功")`

---
### DELETE `/api/article/collect/folder`
- **处理函数**: `CollectRemoveView`
- **所在文件**: `api/article_api/article_collect.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("未找到可删除的收藏夹或无权限")`

---
## 模块: `banner`

### GET `/api/banner`
- **处理函数**: `BannerListView`
- **所在文件**: `api/banner_api/enter.go`
- **请求参数**:
  - 绑定结构体: `PageInfo`
  ```go
  // 结构体 PageInfo 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithList(cached)`

---
### DELETE `/api/banner`
- **处理函数**: `BannerRemoveView`
- **所在文件**: `api/banner_api/enter.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("成功删除%d个")`

---
### POST `/api/banner`
- **处理函数**: `BannerCreateView`
- **所在文件**: `api/banner_api/enter.go`
- **请求参数**:
  - 绑定结构体: `BannerCreateRequest`
  ```go
  Cover  string `json:"cover"`
  Href   string `json:"href"`
  IsShow bool   `json:"isShow"`
  ```
- **成功响应**:
  - `OkWithMsg("上传成功")`

---
### PUT `/api/banner/:id`
- **处理函数**: `BannerUpdateView`
- **所在文件**: `api/banner_api/enter.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("更新成功")`

---
## 模块: `captcha`

### GET `/api/captcha`
- **处理函数**: `CaptchaView`
- **所在文件**: `api/captcha_api/enter.go`
- **请求参数**:
  - 绑定结构体: `CaptchaRequest`
  ```go
  Target string `json:"target" form:"target" query:"target"` //目标业务
  ```
- **成功响应**:
  - `OkWithData(CaptchaResponse{
		CaptchaID: lid)`

---
## 模块: `comment`

### POST `/api/comment`
- **处理函数**: `CommentCreateView`
- **所在文件**: `api/comment_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `CommentCreateRequest`
  ```go
  Content   string `json:"content" binding:"required"`
  ArticleID uint   `json:"articleID" binding:"required"`
  ParentID  uint   `json:"parentID"` // 父评论ID
  ```
- **成功响应**:
  - `OkWithMsg("发布评论成功")`

---
### DELETE `/api/comment/:id`
- **处理函数**: `CommentDeleteView`
- **所在文件**: `api/comment_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("删除评论成功")`

---
### GET `/api/comment`
- **处理函数**: `CommentListlView`
- **所在文件**: `api/comment_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `CommentDetailRequest`
  ```go
  common.PageInfo
  ArticleID uint `form:"articleID" binding:"required"` // 文章ID
  ```
- **成功响应**:
  - `OkWithList(List)`

---
### POST `/api/comment/digg/:id`
- **处理函数**: `CommentDiggView`
- **所在文件**: `api/comment_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithData(claim)`

---
## 模块: `commentChild`

### GET `/api/commentChild`
- **处理函数**: `CommentChildListView`
- **所在文件**: `api/comment_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `CommentListRequest`
  ```go
  common.PageInfo
  Root uint `form:"root" binding:"required"` //根评论ID
  ```
- **成功响应**:
  - `OkWithList(List)`

---
## 模块: `friendLink`

### GET `/api/friendLink`
- **处理函数**: `FriendLinkListView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `PageInfo`
  ```go
  // 结构体 PageInfo 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### DELETE `/api/friendLink`
- **处理函数**: `FriendLinkRemoveView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("成功删除%d个")`

---
### POST `/api/friendLink`
- **处理函数**: `FriendLinkCreateView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `FriendLinkCreateRequest`
  ```go
  Name      string `json:"name"`
  URL       string `json:"url"`
  Logo      string `json:"logo"`
  IsShow    bool   `json:"is_show"`
  SortOrder int    `json:"sort_order"`
  Remark    string `json:"remark"`
  ```
- **成功响应**:
  - `OkWithMsg("添加成功")`

---
### PUT `/api/friendLink/:id`
- **处理函数**: `FriendLinkUpdateView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("更新成功")`

---
## 模块: `friendPromotion`

### GET `/api/friendPromotion`
- **处理函数**: `FriendPromotionListView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `PageInfo`
  ```go
  // 结构体 PageInfo 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### DELETE `/api/friendPromotion`
- **处理函数**: `FriendPromotionRemoveView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("成功删除%d个")`

---
### POST `/api/friendPromotion`
- **处理函数**: `FriendPromotionCreateView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `FriendPromotionCreateRequest`
  ```go
  Title         string   `json:"title"`
  FriendName    string   `json:"friend_name"`
  Avatar        string   `json:"avatar"`
  Category      string   `json:"category"`
  Description   string   `json:"description"`
  PreviewImages string   `json:"preview_images"`
  ContactInfo   []string `json:"contact_info"`
  IsShow        bool     `json:"is_show"`
  SortOrder     int      `json:"sort_order"`
  Position      string   `json:"position"`
  Remark        string   `json:"remark"`
  ```
- **成功响应**:
  - `OkWithMsg("添加成功")`

---
### PUT `/api/friendPromotion/:id`
- **处理函数**: `FriendPromotionUpdateView`
- **所在文件**: `api/friendLink_and_friendPromote/enter.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("更新成功")`

---
## 模块: `heartbeat`

### GET `/api/heartbeat`
- **处理函数**: `Hearth`
- **所在文件**: `api/hearth_api/enter.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithMsg("存活")`

---
## 模块: `image`

### GET `/api/image`
- **处理函数**: `GetImage`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - *返回默认成功或未检测到标准 response 响应*

---
### POST `/api/image`
- **处理函数**: `ImageUploadView`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `Ok(model.ID)`

---
### DELETE `/api/image`
- **处理函数**: `ImageRemoveView`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  IDlist []uint `json:"IDlist" binding:"required"` // 要删除的图片ID列表
  ```
- **成功响应**:
  - `OkWithMsg("图片删除成功)`

---
### GET `/api/image`
- **处理函数**: `GetImage`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - *返回默认成功或未检测到标准 response 响应*

---
### POST `/api/image`
- **处理函数**: `ImageUploadView`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `Ok(model.ID)`

---
### DELETE `/api/image`
- **处理函数**: `ImageRemoveView`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  IDlist []uint `json:"IDlist" binding:"required"` // 要删除的图片ID列表
  ```
- **成功响应**:
  - `OkWithMsg("图片删除成功)`

---
## 模块: `images`

### GET `/api/images`
- **处理函数**: `ImageList`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `PageInfo`
  ```go
  // 结构体 PageInfo 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### GET `/api/images`
- **处理函数**: `ImageList`
- **所在文件**: `api/OSS_img_api/CRUD.go`
- **请求参数**:
  - 绑定结构体: `PageInfo`
  ```go
  // 结构体 PageInfo 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithList(list)`

---
## 模块: `logs`

### GET `/api/logs`
- **处理函数**: `LogListView`
- **所在文件**: `api/log_api/enter.go`
- **请求参数**:
  - 绑定结构体: `LogListRequest`
  ```go
  common.PageInfo
  LogType     enum.LogType  `form:"logType"`
  Level       enum.LogLevel `form:"level"`
  IP          string        `form:"ip"`
  LoginStatus bool          `form:"loginStatus"`
  ServiceName string        `form:"serviceName"`
  UserID      uint          `form:"userID"`
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### GET `/api/logs/:id`
- **处理函数**: `LogReadView`
- **所在文件**: `api/log_api/enter.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("日志已读取")`

---
### DELETE `/api/logs`
- **处理函数**: `LogRemoveView`
- **所在文件**: `api/log_api/enter.go`
- **请求参数**:
  - 绑定结构体: `RemoveRequest`
  ```go
  // 结构体 RemoveRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg(fmt.Sprintf("日志删除成功)`

---
## 模块: `msg`

### GET `/api/msg/conf`
- **处理函数**: `SiteMessageConfView`
- **所在文件**: `api/message_api/enter.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithData(userMessageConf)`

---
### POST `/api/msg/conf/update`
- **处理函数**: `SiteMessageConfUpdateView`
- **所在文件**: `api/message_api/enter.go`
- **请求参数**:
  - 绑定结构体: `UserMessageConfModel`
  ```go
  // 结构体 UserMessageConfModel 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithMsg("更新用户消息配置成功")`

---
### GET `/api/msg/check`
- **处理函数**: `SiteMessageCheckView`
- **所在文件**: `api/message_api/msg_check.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithData(IsRead)`

---
### POST `/api/msg/clear`
- **处理函数**: `OneKeyClearView`
- **所在文件**: `api/message_api/one_key_read.go`
- **请求参数**:
  - 绑定结构体: `OneKeyReadRequest`
  ```go
  CommentMessage        bool `json:"commentMessage"`        // 评论消息
  DiggAndCollectMessage bool `json:"diggAndCollectMessage"` // 点赞收藏消息
  PrivateMessage        bool `json:"privateMessage"`        // 私信消息
  SystemMessage         bool `json:"systemMessage"`         // 系统消息
  ```
- **成功响应**:
  - `OkWithMsg("成功")`

---
### GET `/api/msg`
- **处理函数**: `SiteMessageListView`
- **所在文件**: `api/message_api/msg_list.go`
- **请求参数**:
  - 绑定结构体: `SiteMessageListViewRequest`
  ```go
  common.PageInfo
  Type models.MessageType `form:"type" binding:"required"` //查询的消息类型
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### DELETE `/api/msg`
- **处理函数**: `MessageRemoveView`
- **所在文件**: `api/message_api/msg_remove.go`
- **请求参数**:
  - 绑定结构体: `MessageRemoveRequest`
  ```go
  MessageID []uint `json:"messageID"` // 消息ID//其实使用model.RemoveRequest也是可以的
  ```
- **成功响应**:
  - `OkWithMsg("删除消息成功")`

---
## 模块: `site`

### GET `/api/site/qq_login`
- **处理函数**: `SiteInfoQQView`
- **所在文件**: `api/site_api/enter.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithData(global.Config.QQ.Url())`

---
### GET `/api/site/:name`
- **处理函数**: `SiteInfoView`
- **所在文件**: `api/site_api/enter.go`
- **请求参数**:
  - 绑定结构体: `SiteInfo`
  ```go
  Name string `uri:"name"`
  ```
- **成功响应**:
  - `OkWithData(global.Config.Site)`

---
### PUT `/api/site/:name`
- **处理函数**: `SiteUpdateView`
- **所在文件**: `api/site_api/enter.go`
- **请求参数**:
  - 绑定结构体: `SiteUpdate`
  ```go
  Name string `uri:"name" binding:"required"`
  ```
- **成功响应**:
  - `OkWithMsg("更新站点配置成功")`

---
## 模块: `test`

### GET `/api/test`
- **处理函数**: `TestView`
- **所在文件**: `api/test_api/enter.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - *返回默认成功或未检测到标准 response 响应*

---
## 模块: `print`

### POST `/api/print`
- **处理函数**: `Print`
- **所在文件**: `api/test_api/Target.go`
- **请求参数**:
  - 绑定结构体: `request`
  ```go
  Content string `json:"content"`
  ```
- **成功响应**:
  - `OkWithData(req)`

---
## 模块: `user`

### GET `/api/user/follow/list`
- **处理函数**: `FollowUserListView`
- **所在文件**: `api/follow_api/follow_list.go`
- **请求参数**:
  - 绑定结构体: `FollowUserListRequest`
  ```go
  common.PageInfo
  UserID uint `form:"userID"` // 查用户的关注
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### GET `/api/user/follower/list`
- **处理函数**: `FollowerListView`
- **所在文件**: `api/follow_api/follower_list.go`
- **请求参数**:
  - 绑定结构体: `FollowerListRequest`
  ```go
  common.PageInfo      // 匿名嵌入分页信息
  UserID          uint `form:"userID"` // 被查询的用户ID。如果为0，则查询当前登录用户的粉丝列表；否则查询指定用户的粉丝列表。
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### POST `/api/user/follow`
- **处理函数**: `FollowUserView`
- **所在文件**: `api/follow_api/follow.go`
- **请求参数**:
  - 绑定结构体: `FollowUserRequest`
  ```go
  FocusUserID uint `json:"focusUserID" binding:"required"`
  ```
- **成功响应**:
  - `OkWithMsg("已关注")`

---
### POST `/api/user/follow/unfollow`
- **处理函数**: `UnFollowUserView`
- **所在文件**: `api/follow_api/follow.go`
- **请求参数**:
  - 绑定结构体: `FollowUserRequest`
  ```go
  FocusUserID uint `json:"focusUserID" binding:"required"`
  ```
- **成功响应**:
  - `OkWithMsg("取消关注成功")`

---
### POST `/api/user/send_email`
- **处理函数**: `SendEmailView`
- **所在文件**: `api/user_api/send_email.go`
- **请求参数**:
  - 绑定结构体: `SendEmailRequest`
  ```go
  Type  string `json:"type" binding:"required"`  //注册,重置密码,重置邮箱
  Email string `json:"email" binding:"required"` //注册邮箱
  ```
- **成功响应**:
  - `Ok(SendEmailResponse{
			EmailID:      newEmailID)`

---
### POST `/api/user/email`
- **处理函数**: `RegisterEmailView`
- **所在文件**: `api/user_api/register_email.go`
- **请求参数**:
  - 绑定结构体: `RegisterEmailRequest`
  ```go
  EmailID   string `json:"emailID" binding:"required"`   // 邮箱验证记录的唯一标识ID
  EmailCode string `json:"emailCode" binding:"required"` // 邮箱收到的验证码
  Pwd       string `json:"password" binding:"required"`  // 用户设置的登录密码
  NickName  string `json:"nickName"`                     // 用户昵称（可选）
  ```
- **成功响应**:
  - `OkWithData(map[string]string{
		"AccessToken":  AccessToken)`

---
### POST `/api/user/login`
- **处理函数**: `Login`
- **所在文件**: `api/user_api/login.go`
- **请求参数**:
  - 绑定结构体: `LoginRequest`
  ```go
  Type string `json:"type" binding:"required"` //登录类型,用户名或邮箱登录//TODO:qq登录
  Val  string `json:"val" binding:"required"`
  Pwd  string `json:"pwd" binding:"required"`
  ```
- **成功响应**:
  - `OkWithData(map[string]string{
		"AccessToken":  AccessToken)`

---
### DELETE `/api/user/logout`
- **处理函数**: `LogoutView`
- **所在文件**: `api/user_api/logout.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithMsg("退出登录成功")`

---
### GET `/api/user/detail`
- **处理函数**: `UserDetailView`
- **所在文件**: `api/user_api/user_detail.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithData(result)`

---
### GET `/api/user/info/:id`
- **处理函数**: `CheckUserBaseInfoView`
- **所在文件**: `api/user_api/user_base.go`
- **请求参数**:
  - 绑定结构体: `IDRequest`
  ```go
  // 结构体 IDRequest 定义在其他文件 (可能在 models 包中)
  ```
- **成功响应**:
  - `OkWithData(result)`

---
### GET `/api/user/loginlog`
- **处理函数**: `UserLoginListView`
- **所在文件**: `api/user_api/user_login_log.go`
- **请求参数**:
  - 绑定结构体: `UserLoginListRequest`
  ```go
  common.PageInfo
  UserID    uint   `form:"userId"`
  Ip        string `form:"ip"`
  Addr      string `form:"addr"`
  StartTime string `form:"startTime"` // 起止时间的 年月日时分秒格式
  EndTime   string `form:"endTime"`
  Type      string `form:"type" binding:"required"` // 用户：只能查自己的  管理员 ：能查全部. 使用字符串,更加稳定可控
  ```
- **成功响应**:
  - `OkWithList(list)`

---
### PUT `/api/user/resetEmail`
- **处理函数**: `ResetEmailView`
- **所在文件**: `api/user_api/reset_email.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithMsg("邮箱重置成功")`

---
### PUT `/api/user/update`
- **处理函数**: `UserInfoUpdateView`
- **所在文件**: `api/user_api/user_info_update.go`
- **请求参数**:
  - 绑定结构体: `UserInfoUpdateRequest`
  ```go
  Avatar      *string            `json:"avatar" s-u:"avatar"`
  Abstract    *string            `json:"abstract" s-u:"abstract"`
  LikeTags    *[]string          `json:"likeTags" s-u:"like_tags"`
  NickName    *string            `json:"nickName" s-u:"nick_name"`       // 昵称
  Age         *int               `json:"Age" s-u:"age"`                  // 年龄
  ContactInfo *map[string]string `json:"contactInfo" s-u:"contact_info"` // 联系方式，JSON格式存储
  OpenCollect *bool `json:"openCollect" s-u-c:"open_collect"`  // 公开我的收藏
  OpenFollow  *bool `json:"openFollow" s-u-c:"open_follow"`    // 公开我的关注
  OpenFans    *bool `json:"openFans" s-u-c:"open_fans"`        // 公开我的粉丝
  HomeStyleID *uint `json:"homeStyleID" s-u-c:"home_style_id"` // 主页样式的id
  ```
- **成功响应**:
  - `OkWithMsg("用户信息修改成功")`

---
### PUT `/api/user/admin/update`
- **处理函数**: `AdminUserInfoUpdateView`
- **所在文件**: `api/user_api/admin_user_info_update.go`
- **请求参数**:
  - 绑定结构体: `AdminUserInfoUpdateRequest`
  ```go
  UserID   uint           `json:"userID" binding:"required"`
  Username *string        `json:"username" s-u:"username"`
  Nickname *string        `json:"nickname" s-u:"nickname"`
  Avatar   *string        `json:"avatar" s-u:"avatar"`
  Abstract *string        `json:"abstract" s-u:"abstract"`
  Role     *enum.RoleType `json:"role" s-u:"role"`
  ```
- **成功响应**:
  - `OkWithMsg("用户信息修改成功")`

---
### POST `/api/user/token`
- **处理函数**: `RefreshAccessToken`
- **所在文件**: `api/user_api/refresh_token.go`
- **请求参数**:
  - *无特定结构体绑定（可能是无参或路径参数/Query参数）*
- **成功响应**:
  - `OkWithData(accessToken)`

---
### GET `/api/user/list`
- **处理函数**: `UserListView`
- **所在文件**: `api/user_api/user_list.go`
- **请求参数**:
  - 绑定结构体: `UserListRequest`
  ```go
  common.PageInfo
  ```
- **成功响应**:
  - `OkWithList(list)`

---
